name: clickup-mcp-server-better
runtime: container

startCommand:
  type: http
  command: ["npm", "run", "start"]
  env:
    ENABLE_SSE: "true"
    ENABLE_STDIO: "false"
    PORT: "8081"

installCommand: ["npm", "ci"]
transport: streamable-http
port: 8081
path: /mcp